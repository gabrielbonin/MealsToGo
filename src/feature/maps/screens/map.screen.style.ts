import styled from "styled-components/native";

import MapView, { Marker } from "react-native-maps";

export const Map = styled(MapView)`
  width: 100%;
  height: 100%;
`;

export const MarkerPoint = styled(Marker)``;
