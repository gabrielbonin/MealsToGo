import styled from "styled-components/native";
import { MapCallout } from "react-native-maps";

export const MapCalloutView = styled(MapCallout)``;

export const MapCalloutText = styled.Text`
  font-size: 16px;
`;
